apiVersion: v1
kind: Service
metadata:
  name: ml-sentiment-app
spec:
  type: ClusterIP
  selector:
    app: ml-sentiment-app
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
